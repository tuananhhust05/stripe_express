<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head', { title }) %>
  <body style="background: var(--bg-alt); min-height: 100vh; display: flex; align-items: center; justify-content: center; padding: 2rem;">
    <div style="width: 100%; max-width: 500px; background: var(--bg-card); border-radius: 20px; padding: 2.5rem; box-shadow: 0 8px 32px var(--shadow-lg); border: 1px solid var(--border-light); text-align: center;">
      <div style="font-size: 4rem; margin-bottom: 1rem;">✅</div>
      <h1 style="margin: 0 0 1rem 0; color: var(--text); font-size: 1.75rem; font-weight: 600;">Payment Successful!</h1>
      <p style="margin: 0 0 2rem 0; color: var(--muted); font-size: 1rem;">Your subscription has been successfully activated.</p>
      
      <% if (user) { %>
        <div style="margin-top: 2rem;">
          <a href="/subscription" class="btn primary" style="padding: 0.75rem 1.75rem; text-decoration: none; display: inline-block;">Manage Subscription</a>
        </div>
      <% } else { %>
        <div style="margin-top: 2rem;">
          <p style="color: var(--muted); margin-bottom: 1rem; font-size: 0.9rem;">Please login to manage your subscription</p>
          <a href="/login" class="btn primary" style="padding: 0.75rem 1.75rem; text-decoration: none; display: inline-block; margin-right: 0.5rem;">Login</a>
          <a href="/register" class="btn ghost" style="padding: 0.75rem 1.75rem; text-decoration: none; display: inline-block;">Sign Up</a>
        </div>
      <% } %>

      <p style="margin-top: 2rem; text-align: center;">
        <a href="/" style="color: var(--accent); text-decoration: none;">← Back to site</a>
      </p>
    </div>

    <script>
      // Auto-redirect to subscription page if user is logged in (has cookie)
      <% if (user) { %>
        setTimeout(() => {
          window.location.href = '/subscription';
        }, 2000);
      <% } %>
    </script>
  </body>
</html>


